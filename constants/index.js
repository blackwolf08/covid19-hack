import * as theme from './theme';
import * as mocks from './mocks';
import React from 'react';
import { Dimensions } from 'react-native';
const { width, height } = Dimensions.get('window');

export { theme, mocks, width, height };
